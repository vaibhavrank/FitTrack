# FitTrack Frontend

This frontend is built with Vite + React and connects to a FastAPI backend for authentication.

## Environment Setup

Copy `.env.example` to `.env` and provide your Google OAuth Client ID:

```bash
cp .env.example .env
# then edit .env and set VITE_GOOGLE_CLIENT_ID
```

## Running

```bash
npm install
npm run dev
```
